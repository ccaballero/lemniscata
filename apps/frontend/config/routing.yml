templates_list:
  url: templates.html
  param: { module: templates, action: index }

templates_template_view:
  url: templates/:id/view.html
  param: { module: templates, action: view }

homepage:
  url:   /
  param: { module: frontpage, action: index }

# generic rules
# please, remove them by adding more specific rules
default_index:
  url:   /:module
  param: { action: index }

default:
  url:   /:module/:action/*

